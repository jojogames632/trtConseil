<div class="row d-flex justify-content-center">
	<div class="col-9">
		<table class="table table-hover text-center">
			<thead>
				<tr>
					<th>Prénom</th>
					<th>Nom</th>
					<th>CV</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				{% for user in pendingCandidates %}
					<tr>
						<td>{{ user.firstName }}</td>
						<td>{{ user.lastName }}</td>
						<td>{{ user.cvFilename }}</td>
						<td>
							<a href="{{ path('activate_candidate', {'id': user.id}) }}">
								<button type="button" class="btn btn-success">Activer</button>
							</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
</div>